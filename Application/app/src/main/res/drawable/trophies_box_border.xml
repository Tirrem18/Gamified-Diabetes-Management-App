<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/holo_green_dark"/> <!-- Inner background (Yellow) -->
    <stroke android:width="4dp" android:color="@android:color/black"/> <!-- Black border -->
    <corners android:radius="16dp"/> <!-- Rounded corners -->
</shape>
